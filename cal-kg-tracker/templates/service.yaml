apiVersion: v1
kind: Service
metadata:
  name: {{ include "cal-kg-tracker.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "cal-kg-tracker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cal-kg-tracker.selectorLabels" . | nindent 4 }}
